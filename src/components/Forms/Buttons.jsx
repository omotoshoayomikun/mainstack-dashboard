import React from "react"

export const Btn1 = (props) => {
    return(
        <>
        <button className="text-my-orange border-0 cursor-pointer text-sm">{props.title}</button>
        </>
    )
}